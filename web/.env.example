# Server-side only. Use a PostgreSQL role with SELECT-only access in production.
# Never prefix this value with NEXT_PUBLIC_.
DATABASE_URL=postgresql://job_radar:replace-with-a-long-random-password@localhost:5432/job_radar
